version: '3'
services:
  odoo:
    image: busybox
    command: 'chown 101:101 -R /data /checksums'
    volumes:
      - odoo-filestore:/data
      - odoo-checksums:/checksums
volumes:
   odoo-filestore:
   odoo-checksums:
